{
  "name": "otp-bridge-relay-worker",
  "version": "1.0.0",
  "private": true,
  "description": "OTP Bridge relay on Cloudflare Workers + Durable Object (free tier, no credit card).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
